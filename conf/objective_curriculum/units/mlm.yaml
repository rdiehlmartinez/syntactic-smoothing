mlm: 
  task_head_params: {
    "tie_weights": true,
  }

  optimizer_params: { 
    "lr": 1e-3,
  }

  scheduler_params: {}

  optional_kwargs: { 
    "mask_probability": 0.15,
    "unmask_probability" : 0,
    "label_smoothing": 0.5, # 0.2 means 0.8 for the masked token and 0.2 for each of the rest
  }
